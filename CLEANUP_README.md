# 🧹 Скрипты очистки базы данных

Этот набор скриптов предназначен для очистки базы данных от "мусорных" записей - файлов, которые были помечены как удаленные или физически отсутствуют на диске.

## 📋 Что удаляется

Скрипты находят и удаляют файлы, которые:

1. **Помечены как удаленные** (`is_deleted=True`) - файлы, которые были "мягко" удалены
2. **Физически отсутствуют на диске** - файлы, записи о которых есть в БД, но сами файлы удалены
3. **Имеют отсутствующие QR коды** - файлы, у которых QR коды были удалены

## 🚀 Способы запуска

### 1. Django Management команда (рекомендуется)

```bash
# Просмотр файлов для удаления (без фактического удаления)
python manage.py cleanup_deleted_files --dry-run

# Удаление с подтверждением
python manage.py cleanup_deleted_files

# Принудительное удаление без подтверждения
python manage.py cleanup_deleted_files --force

# Включить в очистку постоянные файлы
python manage.py cleanup_deleted_files --include-permanent
```

### 2. Прямой Python скрипт

```bash
# Просмотр файлов для удаления
python cleanup_deleted_files.py --dry-run

# Удаление с подтверждением
python cleanup_deleted_files.py

# Принудительное удаление
python cleanup_deleted_files.py --force

# Включить постоянные файлы
python cleanup_deleted_files.py --include-permanent
```

## ⚙️ Параметры

| Параметр | Описание |
|----------|----------|
| `--dry-run` | Показать, какие файлы будут удалены, без фактического удаления |
| `--force` | Принудительно удалить все найденные записи без подтверждения |
| `--include-permanent` | Включить в очистку постоянные файлы (по умолчанию они исключены) |

## 🔒 Безопасность

- **Постоянные файлы** (`is_permanent=True`) по умолчанию **НЕ удаляются**
- Всегда сначала запускайте с `--dry-run` для просмотра
- Используйте `--force` только когда уверены в результате
- Операции выполняются в транзакции - при ошибке изменения откатываются

## 📊 Пример вывода

```
🧹 Начинаем очистку базы данных от удаленных файлов...
📋 Найдено файлов, помеченных как удаленные: 2
📋 Найдено файлов, отсутствующих на диске: 2
📋 Найдено QR кодов, отсутствующих на диске: 2

📊 Детали файлов для удаления:
   • Помеченные как удаленные: 2
   • Отсутствующие на диске: 2
   • С отсутствующими QR кодами: 2
   • Всего уникальных файлов: 2

🗑️  Удаляем 2 файлов...
   Удаляем: 321 - lexus.jpg
   Удаляем: 123456 - lexus.jpg

📈 Результаты очистки:
   ✅ Успешно удалено: 2
   ❌ Ошибок: 0

🎉 Очистка завершена! Удалено 2 файлов.
```

## 🛠️ Технические детали

- Скрипты используют кастомный метод `delete()` модели `File`
- При удалении физически удаляются файлы и QR коды с диска
- Коды файлов освобождаются для повторного использования
- Записи полностью удаляются из базы данных
- Все операции логируются

## 🔄 Рекомендации по использованию

1. **Регулярная очистка**: Запускайте скрипт периодически (например, раз в неделю)
2. **Перед обновлением**: Очищайте БД перед развертыванием обновлений
3. **Мониторинг**: Следите за количеством "мусорных" записей
4. **Бэкапы**: Делайте резервные копии БД перед массовой очисткой

## ⚠️ Важные замечания

- Удаленные файлы **НЕ ВОССТАНАВЛИВАЮТСЯ**
- Убедитесь, что у вас есть резервные копии важных данных
- Тестируйте скрипты на тестовой среде перед продакшеном
